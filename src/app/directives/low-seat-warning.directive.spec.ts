import { LowSeatWarningDirective } from './low-seat-warning.directive';

describe('LowSeatWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new LowSeatWarningDirective();
    expect(directive).toBeTruthy();
  });
});
